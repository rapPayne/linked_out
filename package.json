{
  "name": "linked_out",
  "version": "0.0.1",
  "description": "Linked In clone for ADP",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --watch server.js",
    "test": "node --experimental-vm-modules ./node_modules/.bin/jest --watch",
    "load-db": "node --experimental-strip-types make_random_people.ts"
  },
  "author": "Better cohort",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.8.0"
  },
  "engines": {
    "node": "^22"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
